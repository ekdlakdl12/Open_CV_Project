<Window x:Class="WpfApp11.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp11"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
	<Grid Margin="15">
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<!-- 이미지 영역 -->
			<RowDefinition Height="Auto"/>
			<!-- 버튼 영역 -->
		</Grid.RowDefinitions>

		<Grid Grid.Row="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<!-- 원본 사진 영역 -->
			<GroupBox Header="원본 사진" Grid.Column="0" Margin="5">
				<Border Background="#EEEEEE" CornerRadius="5">
					<Image x:Name="imgOriginal" Stretch="Uniform"/>
				</Border>
			</GroupBox>

			<!-- 처리 결과 사진 영역 -->
			<GroupBox Header="검출 결과 (8배 업스케일)" Grid.Column="1" Margin="5">
				<Border Background="#EEEEEE" CornerRadius="5">
					<Image x:Name="imgResult" Stretch="Uniform"/>
				</Border>
			</GroupBox>
		</Grid>

		<!-- 하단 버튼 -->
		<Button x:Name="BtnLoad" Grid.Row="1"
                Content="이미지 불러오기 및 자동 검출"
                Width="300" Height="50" Margin="0,15,0,0"
                FontSize="18" FontWeight="Bold"
                Click="BtnLoad_Click"/>
		<TextBox HorizontalAlignment="Left" Margin="575,0,0,0" Grid.Row="1" TextWrapping="Wrap"  VerticalAlignment="Center" Width="120" x:Name="ResultTextBox"/>
	</Grid>
</Window>
